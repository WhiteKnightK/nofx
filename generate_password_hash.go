package main

import (
	"fmt"
	"os"

	"golang.org/x/crypto/bcrypt"
)

func main() {
	password := "#Jzh63111"
	
	// 使用与系统相同的bcrypt配置（cost=10）
	hash, err := bcrypt.GenerateFromPassword([]byte(password), 10)
	if err != nil {
		fmt.Printf("❌ 生成密码哈希失败: %v\n", err)
		os.Exit(1)
	}
	
	fmt.Println("╔════════════════════════════════════════════════════════════╗")
	fmt.Println("║           密码哈希生成工具                                  ║")
	fmt.Println("╚════════════════════════════════════════════════════════════╝")
	fmt.Println()
	fmt.Printf("原始密码: %s\n", password)
	fmt.Println()
	fmt.Println("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
	fmt.Println("✅ bcrypt 哈希值（可直接写入数据库）:")
	fmt.Println("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
	fmt.Println()
	fmt.Printf("%s\n", string(hash))
	fmt.Println()
	fmt.Println("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
	fmt.Println()
	fmt.Println("📝 SQL更新语句示例:")
	fmt.Println("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
	fmt.Println()
	fmt.Printf("UPDATE users SET password_hash = '%s' WHERE email = '你的邮箱';\n", string(hash))
	fmt.Println()
	fmt.Println("💡 提示:")
	fmt.Println("   - 这个哈希值可以直接复制到数据库的 password_hash 字段")
	fmt.Println("   - 每次生成的哈希值都不同（因为包含随机盐值）")
	fmt.Println("   - 但都能验证同一个密码")
}

